rf2o_laser_odometry:
  ros__parameters:
    laser_scan_topic: /scan
    odom_topic: /odom/rf2o
    base_frame_id: base_link
    odom_frame_id: odom
    publish_tf: false
    # Otherwise upstream waits for /base_pose_ground_truth and ignores scans.
    init_pose_from_topic: ""
    freq: 10.0
